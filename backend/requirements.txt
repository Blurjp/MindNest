# FastAPI and server
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
python-multipart==0.0.6

# CORS and middleware
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Optional: AI and ML services (for production)
# openai==1.3.0              # For GPT-5 and Whisper
# anthropic==0.7.0            # For Claude API
# elevenlabs==0.2.24          # For TTS generation
# transformers==4.35.0        # For local emotion detection
# torch==2.1.0                # For running local models

# Optional: Database (for production)
# sqlalchemy==2.0.23
# psycopg2-binary==2.9.9      # PostgreSQL
# alembic==1.12.1             # Database migrations

# Development
pytest==7.4.3
httpx==0.25.2
